// @flow

type scopeTest$Options = {
  requiredScopes?: string[];
  ignoreCase?: boolean;
};

declare export class UngrantedScopeError extends Error {
  missingScopes: string[];
}

declare export default (scopes: string[], options?: scopeTest$Options) => void;